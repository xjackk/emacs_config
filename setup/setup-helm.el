;;; package ---  setup-helm.el
;;; Commentary:
;;; setup-helm.el -*- lexical-binding: t; -*-

;;; Code:

(require 'helm-config)
(helm-mode 1) ;; global helm mode
(helm-autoresize-mode t) ;; auto resize

(provide 'setup-helm)
;;; setup-helm ends here

